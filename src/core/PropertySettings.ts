// TODO add displayValue and value as allowedvalues to accomodate for path in folder/project and HR values vs IDs
export type PropertySettings = {
    allowedValues?:string[],
    defaultValue:string
}