export enum Context{
    desk="desk",
    phone="phone",
    read="read",
    deep_thinking="deep_thinking"
}

export enum Status{
    inbox="inbox",
    next="next",
    waiting_for="waiting_for",
    deferred="deferred"
}

export enum FilePropertiesMap{
    status="status",
    context="context"
}
