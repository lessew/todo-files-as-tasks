export enum Context{
    desk="desk",
    phone="phone",
    read="read",
    deep_thinking="deep_thinking",
    none="none"
}

export enum Status{
    inbox="inbox",
    next="next",
    waiting_for="waiting_for",
    deferred="deferred",
    none="none"
}

export enum FilePropertiesMap{
    status="status",
    context="context"
}
