export {};
import { CodeBlockProcessor } from "src/CodeBlockProcessor";




describe('testing index file', () => {
  //let testElement = 


  CodeBlockProcessor.process("this",new HTMLElement());
    test('empty string should result in zero', () => {
      expect(1).toBe(0);
    });
  });